/*
TODO:
переделать все под функцию LeftWheelMove
extern void LeftWheelForward(int speed);
extern void LeftWheelBackward(int speed);
*/
void simple_control() {
/*if (Serial.available() > 0) {
    char command = Serial.read();  // Читаем команду

    switch (command) {
      case 'F':             // Вперед
        LeftWheelForward(100); 
        RightWheelForward(100);
        break;
      case 'B':             // Назад
        LeftWheelBackward(100);  
        RightWheelBackward(100);
        break;
      case 'L':            // Влево
        LeftWheelForward(50); 
        RightWheelBackward(50);
        break;
      case 'R':            // Вправо
        LeftWheelBackward(50);
        RightWheelForward(50);
        break;
      case 'S':            // Вправо
        LeftWheelBackward(0);  
        RightWheelForward(0);
        break;
      default:
        break;
    }
  }*/
}


